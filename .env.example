# Copy to .env and fill values.
#
# Backend
BACKEND_PORT=8080
BACKEND_BIND_ADDRESS=127.0.0.1
DB_PATH=./data/pasarela.db
FRONTEND_BASE_URL=http://localhost:3000
JWT_SECRET=dev-jwt-secret-please-change-me-32-chars-minimum
# 32 bytes base64 (example generation):
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_ENCRYPTION_KEY_BASE64=NAyhesJG7rrNDRTEck3YiAZ11/nX6Bg08zqRBjPn4L8=

# Stripe
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Adyen
ADYEN_API_KEY=your_adyen_api_key
ADYEN_MERCHANT_ACCOUNT=your_merchant_account
ADYEN_CLIENT_KEY=your_client_key
ADYEN_HMAC_KEY=your_notification_hmac_key_base64
ADYEN_ENVIRONMENT=test

# Frontend
# If unset, the frontend will call relative `/api/*` and Next.js will proxy to the backend (no CORS).
# Set this only if your frontend must call the backend directly (e.g., deployed separately).
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
